node_modules/
dist/
coverage/
public/.og-image.generated
playwright-data/
playwright-report/
test-results/
*.local

# Linkrot guard result cache (7-day TTL; regenerated by `npm run test:links`)
scripts/linkrot-cache.json

# Ad-hoc probe scripts dropped at the repo root during QA — never commit
.probe-*

# Local security-audit notes; contain findings that must not be public
security-followups.md
