#!/usr/bin/env bash
# Keep commit subjects short and non-empty.
set -euo pipefail

subject="$(head -n 1 "$1")"
if [ -z "$subject" ]; then
  echo "commit-msg: empty commit subject"
  exit 1
fi
if [ "${#subject}" -gt 72 ]; then
  echo "commit-msg: subject exceeds 72 characters"
  exit 1
fi
